import { TrabajadoresPipe } from './trabajadores.pipe';

describe('TrabajadoresPipe', () => {
  it('create an instance', () => {
    const pipe = new TrabajadoresPipe();
    expect(pipe).toBeTruthy();
  });
});
